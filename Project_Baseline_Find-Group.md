# 📄 Project Baseline — Find-Group

**Версія:** 1.1  
**Останнє оновлення:** 2025-10-30  
**Автор:** OlegIvakhiv

---

## 1. Вступ

* **Назва проєкту:** Find-Group
* **Короткий опис:** Веб-сервіс для пошуку напарників (LFG/Matchmaking) для онлайн-ігор. Користувачі створюють/шукають лобі з фільтрами по грі, ролі, часу та рейтингу; сервіс забезпечує реальний час оновлень і безпечну систему репутації.
* **Мета проєкту:** Забезпечити зручний та надійний спосіб знайти команду або гравців для ігор, з пріоритетом інтеграцій (Discord/Steam) та захистом від фейків/спаму.
* **Цільова аудиторія:** Геймери (casual → competitive), клан-лідери, організатори івентів.

---

## 2. Scope & Requirements

### 2.1. MVP — Основні функції (Must)

| № | Функціонал | Ключові Деталі |
| :--- | :--- | :--- |
| **1** | **Реєстрація/логін** | Email/password (опціонально) + **OAuth** (Discord, Steam, Google). Email verification (для production). |
| **2** | **Створення та пошук лобі** | Поля: гра, режим, платформа, min/max гравців, рейтинг/рівень, мова, час початку, приват/публічне. **Фільтри та сортування** лобі. |
| **3** | **Чат (Realtime)** | Текстовий чат у лобі та приватні повідомлення через **WebSocket**. |
| **4** | **Система репутації** | Базова система відгуків (`thumbs up/down`), можливість зробити `report`. |
| **5** | **Нотифікації** | Web Push, email (опціонально). |
| **6** | **Профіль користувача** | Статистика, зовнішні теги (Steam ID, Discord), налаштування приватності. |
| **7** | **Адмін панель** | Модерація звітів (`reports`), бан/тайм-аут користувачів, лог дій модераторів. |

### 2.2. Пост-MVP (v2/v3 — Should/Could)

* **v2 (Should):** Голосовий чат (інтеграції), матчмейкінг по skill/MMR (після збору даних).
* **v3 (Could):** Promoted lobbies, PWA / мобільні клієнти.

### 2.3. Нефункціональні вимоги

| Категорія | Вимога | Цільовий Показник |
| :--- | :--- | :--- |
| **Продуктивність** | Латентність чату (p95) | **< 300 ms** |
| **Масштабованість** | Concurrent users (початково) | До **10,000** |
| **Безпека** | Захист | TLS everywhere, OAuth, CSRF/XSS, dependency scanning. |
| **Відмовостійкість** | RPO / RTO | RPO (втрата даних) 15 хв; RTO (час відновлення) 1 год. |

### 2.4. Обмеження Проєкту

* **Час:** Запуск MVP — не пізніше кінця **3-го місяця** (включно з 2-тижневим резервом).
* **Ресурси:** Максимум **5 FTE** (повних еквівалентів).
* **Бюджет:** Цільовий максимум **$65,000** (включає резерв 20%).

---

## 3. High-level архітектура

Проєкт реалізується як мікросервісний або багатокомпонентний додаток:

* **Frontend:** React + TypeScript (Static hosting / Vercel).
* **Backend API:** Node.js (NestJS/Express) — REST/GraphQL.
* **Realtime Service:** WebSocket / Socket.IO (або managed service).
* **Auth Service:** OAuth handlers + session/token management.
* **Database:** PostgreSQL (Managed service).
* **Cache / PubSub:** Redis (sessions, rate limiting, pub/sub для realtime).
* **Storage:** S3 / DigitalOcean Spaces (для аватарок).
* **Worker Queue:** BullMQ / Redis Queue (для асинхронних задач: email, нотифікації).

---

## 4. Acceptance Criteria & Definition of Done (DoD)

### 4.1. Acceptance Criteria (Приклад)

* **Створення лобі:** Користувач може створити лобі з мінімальною формою. Лобі отримує унікальний ID і з’являється у списку протягом **1 секунди**.
* **Chat:** Повідомлення передається між учасниками через WebSocket; latency **p95 < 300 ms** під помірним навантаженням.
* **Admin:** Модератор може подивитись і закрити `report`; дія зберігається в audit log.

### 4.2. Definition of Done (DoD)

Кожна розроблена функція вважається "Готовою", якщо виконані наступні умови:

1.  Пройдені **Unit Tests** та інші автоматизовані тести (у CI pipeline).
2.  Пройдений **Code Review** (мінімум 1 approval від Tech Lead/колеги).
3.  **Документація** endpoint (OpenAPI) оновлена.
4.  Здійснений **Деплой на staging** + пройдені **Smoke Tests**.
5.  Створені міграційні скрипти для DB (за потреби).

---

## 5. API Skeleton (Основні Endpoint'и)

| Група | Метод | Endpoint | Призначення |
| :--- | :--- | :--- | :--- |
| **Auth** | `POST` | `/auth/oauth/discord` | Початок OAuth-авторизації |
| | `POST` | `/auth/email/register` | Реєстрація через email |
| **Lobbies** | `POST` | `/lobbies` | Створити нове лобі |
| | `GET` | `/lobbies` | Отримати список лобі (з фільтрами) |
| | `POST` | `/lobbies/{id}/join` | Приєднатися до лобі |
| **Users** | `GET` | `/users/me` | Отримати дані про поточного користувача |
| **Reports** | `POST` | `/reports` | Відправити звіт про користувача/лобі |

*(Повна OpenAPI специфікація надається як додаток.)*

---

## 6. Data Model (ERD Skeleton)

Основні сутності (Entities) та їхні зв'язки:

* **users:** `id`, `username`, `email`, `external_ids` (Discord/Steam), `reputation`.
* **lobbies:** `id`, `host_user_id` (FK), `game`, `mode`, `min_players`, `max_players`, `status`.
* **lobby_members:** `lobby_id` (FK), `user_id` (FK), `role`, `joined_at`.
* **messages:** `lobby_id` (FK), `user_id` (FK), `content`, `created_at`.
* **reports:** `reporter_id` (FK), `target_type`, `reason`, `status`.

---

## 7. Schedule (Графік) — 3 місяці

| Місяць | Тривалість | Основна Діяльність | Deliverables |
| :--- | :--- | :--- | :--- |
| **Місяць 1** | 4 тижні | **Discovery & Design & Spikes** (Вимоги, UI/UX, OAuth POC, WebSocket POC, Infra Setup). | Figma prototype, OpenAPI skeleton, Infra POC. |
| **Місяць 2** | 4 тижні | **Core Development** (Backend Auth + Lobby CRUD, Realtime Service, Frontend basic flows, CI/CD). | Staging MVP (end-to-end happy path). |
| **Місяць 3** | 4 тижні | **QA & Hardening & Deploy** (OAuth integrations, Testing, Security, Production deploy, Handover). | Production release MVP, Final report. |

---

## 8. Ресурси та Бюджет

### 8.1. Команда (Баланс 5 FTE)

| Роль | FTE | Години (3 місяці) |
| :--- | :--- | :--- |
| Product Manager (PM) | 0.25 | ~60 h |
| Tech Lead (TL) | 0.75 | ~360 h |
| Frontend Devs | 2.0 | ~960 h |
| Backend Dev | 1.0 | ~480 h |
| QA | 0.5 | ~240 h |
| UI/UX | 0.5 | ~120 h |
| **Разом** | **5.0** | **~2220 h** |

### 8.2. Фінансова Оцінка

* **Трудозатрати (еквівалент):** 2220 h \* $25/h ≈ **$55,500**
* **Резерв (20%):** ≈ **$11,100**
* **Загальна цільова вартість:** **$66,600** (в межах цільового бюджету ~$65k).
* **Інфраструктура (MVP):** ~$50–$150 / місяць (використання Free Tiers для початку).

---

## 9. CI/CD та Процеси

* **Workflow:** GitFlow (feature → develop → main).
* **CI:** GitHub Actions. Запуск `lint + unit tests` на кожен PR.
* **CD:** Автоматичний деплой на Staging. **Production** вимагає **ручного затвердження**.
* **DB Migrations:** Використання міграцій (TypeORM/Flyway), що відслідковуються у CI.

---

## 10. Security & Anti-fraud Чекліст

1.  **Аутентифікація:** Пріоритет OAuth (Discord/Steam) для зниження кількості фейків.
2.  **Захист даних:** `bcrypt/argon2` для хешування паролів. Secure headers (HSTS, CSP).
3.  **DDoS/Спам:** Rate limiting (Redis) та базова WAF-захист.
4.  **Код:** Dependency scanning (Dependabot / Snyk), Audit logs для адмін-дій.

---

## 11. Monitoring, Alerting та SLOs

| Метрика | Цільовий Показник (SLO) | Інструменти |
| :--- | :--- | :--- |
| **Availability** | Uptime Target | **99.9%** (для core endpoints) |
| **Performance (Read)** | API p95 Latency | **< 200 ms** |
| **Error Rate** | 5xx Errors | **< 1%** на production |
| **Business KPI** | Time to fill lobby | **< 10 хв** |

* **Alerts:** Налаштування критичних сповіщень у Sentry (Errors) та Grafana (Metrics) з надсиланням у Slack/Discord.
* **Backups:** Автоматичні щоденні бекапи DB; RTO (час відновлення) ціль: 1 година.

---

## 12. Privacy, Legal & Compliance

* Необхідна **Політика конфіденційності** та **Terms of Service** (шаблони).
* Обов'язкова **Згода** користувача на обробку персональних даних.
* Зовнішні ID (Steam/Discord) не повинні публікуватись без згоди користувача.

---

## 13. Risks & Mitigations

| Ризик | Опис | Mitigation Plan |
| :--- | :--- | :--- |
| **R1** | Затримка Realtime (Latency) | Проведення **Tech Spike** (POC) у Фазі 1; використання Managed Realtime Service (як fallback). |
| **R2** | OAuth Integration Delays | Поступова інтеграція, пріоритет **Discord** як основного. |
| **R3** | Scope Creep (розширення обсягу) | Чіткий розподіл на **Must/Should/Could**; контроль формальних **Change Requests**. |
| **R4** | Масштабування (навантаження) | Stateless архітектура, Redis pub/sub, **Load Testing** (k6) на Фазі 3. |

---

## 14. Roadmap v2 (KPI & Релізи)

| Реліз | Час після MVP | Основний Функціонал | Ключові KPI |
| :--- | :--- | :--- | :--- |
| **v1 (MVP)** | 0 місяців | LFG, Chat, OAuth (Discord/Steam), Admin. | 1k MAU, Time to fill < 10 min, Retention D1 ≥ 30%. |
| **v2** | 3–6 місяців | MMR Matching, Promoted Lobbies, Discord Bot Integration. | Retention D7 ≥ 15%, Conversion Join Rate ↑ 15%. |
| **v3** | 6+ місяців | Voice Integrations, Mobile Apps, Монетизація. | |

---

## 15. Deliverables

* Project Baseline (цей документ).
* Figma prototype (key screens).
* OpenAPI skeleton (`openapi.yaml`).
* Component UML (`component_uml.png`).
* Gantt timeline (`gantt.png`).
* CI workflow (`github-actions.yml`).

---

## 16. To Do (Наступні кроки)

1.  Підготувати OpenAPI файл та basic Postman collection.
2.  Накреслити ERD у Draw.io / Figma.
3.  Налаштувати repo + GitHub Actions та базовий Staging deploy.
4.  Підготувати презентацію (12–16 слайдів) для захисту.

---